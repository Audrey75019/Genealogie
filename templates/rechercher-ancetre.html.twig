{% extends 'base.html.twig' %}

{% block title %}

{% endblock %}

{% block body %}
<h1>rechercher un ancetre</h1>
    {{ form_start(createForm) }}
    {{ form_widget(createForm) }}
    <input type="submit" value="Modifier" class="btn btn-outline-primary">
    {{ form_end(createForm) }}

{% endblock %}